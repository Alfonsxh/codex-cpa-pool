# Changelog

本项目从 `v1.1.0` 开始维护面向使用者的变更记录。完整提交历史可在 GitHub 查看。

## v2.0.9 - 2026-09-15

- 修复停用账号时因等待旧请求排空而长时间无响应的问题；停用只更新选择策略与用户路由，保留账号容器和正在进行的请求。
- 提交时显示“正在停用”，阻止重复提交；失败后显示原因并允许重试。
- 账号列表刷新时保留已选择的迁移目标，并补齐策略发布失败回滚和中断恢复测试。

## v2.0.7-rc.1 - 2026-09-12（Pre-release）

- 活跃用户统计默认改为滚动 15 分钟，并新增 `usage.active_user_window_seconds` 配置（60–86400 秒）。
- 管理中心、Usage、自动切换和企业微信通知统一使用配置窗口，页面和通知按实际窗口动态显示。
- API 返回 `active_user_window_seconds` 元数据，保留既有 `active_users_1h` 字段兼容旧客户端。

## v2.0.6 - 2026-09-10

- 管理中心、总览和使用中心共用账号状态、说明及可切换判断，避免同一账号在不同页面显示不同结论。
- 两处账号列表统一缓存、每 15 秒前台更新和返回页面取数；手动刷新共用后台额度查询，等待期间每 3 秒更新。
- 企业微信报告与额度事件统一为完整表格，按周额度已用比例升序排列；补齐重置次数、下次周期重置及事件前后用量，保持系统时区转换。
- 包含 rc.1 的过期 CPA 冷却自动恢复、周额度耗尽时的重置入口修复及“剩余 N 次”提示。

## v2.0.6-rc.1 - 2026-09-10（Pre-release）

- 修复官方周额度进入新周期后，CPA 仍保留旧冷却状态导致使用中心无法切换的问题；自动恢复会核对账号身份、额度周期及当前运行状态。
- 周额度已用 100% 且有重置余额时允许发起重置，不再因上游提示字段不一致而提前拦截；提交前重新核对周额度和所选重置次数。
- 重置次数改为“剩余 N 次”，区分没有余额与周额度尚未耗尽；上游拒绝重置时显示失败，不自动重试。

## v2.0.4 - 2026-09-09

- 新增 XLSX 用量周报，汇总团队、账号、个人及每日趋势，展示核心指标、图表和环比。
- 明细补充输入、缓存、输出 Token、当前绑定关系及最后请求时间，保留历史加权值。
- 导出支持自定义起止时间和带单位／不带单位选项；统一表头对齐、冻结、筛选和排序，保留可计算数值。
- 企业微信额度报告按本周使用率降序展示；管理中心兼容旧版预发布编号。

## v2.0.4-rc.3 - 2026-09-09（Pre-release）

- 重设计 XLSX 用量总览，增加核心指标、每日与团队图表、本周观察。
- 账号、用户明细补充输入、缓存、输出 Token、当前绑定关系及最后请求时间。
- 统一表头与内容对齐，固定表头，按原始 Token 降序；占比、环比和人均同步使用原始口径。
- 当前绑定账号与历史用量归属分开统计，保留历史加权值、无请求记录和数值格式选项。

## v2.0.4-rc.2 - 2026-09-09（Pre-release）

- 周报导出弹窗分开展示开始、结束时间，统一按钮布局，并常显统计口径。
- 新增带单位／不带单位选项；XLSX 支持 K/M/B 展示，保留完整数值用于计算。
- 预发布改用标准版本编号，顺序为 `v2.0.3 < v2.0.4-rc.1 < v2.0.4-rc.2 < v2.0.4`。

## v2.0.4-rc.1 - 2026-09-09（Pre-release）

- 新增 XLSX Token 周报，包含总览、团队、账号、个人和每日趋势；支持历史周、本周及环比。
- 周报保留原始与历史加权 Token，按当前团队归属汇总，去重人数并保留历史身份用量。
- 企业微信额度报告按本周使用率降序展示。
- 周报模块纳入 Control 镜像摘要；预发布镜像保留正式版 `latest`。
- 管理中心兼容 `.rc.1` 格式的部署版本显示与比较。

## v2.0.3 - 2026-09-08

- 修复 CPA 配置更新后文件监听失效的问题，保留新用户内部 Key 的热加载能力。
- 新建用户在账号认证成功后才发布 Gateway Key，激活失败时回滚创建。
- 账号管理分隔时间筛选与搜索操作区；用户管理的 CPA 数量移至第二行。
- 精简发布公告和项目文档，修复软链接路径下的 Telegram 通知入口。

## v2.0.2 - 2026-09-08

- 账号管理新增模型通信测试，统一账号与用户的时间筛选。
- 修复企业微信调度启动，分别展示后台心跳和发送结果。
- 优化 SQLite 在线备份，排除日志、历史备份和生成状态。
- 本机发布工具支持正式版 Telegram 公告、产物校验、去重与重试。

升级方式见[升级](docs/upgrade.md)。模型测试会产生用量，历史加权 Token 不重算；企业微信沿用已有发送计划，Telegram 在发布工作站单独配置。

## v2.0.1 - 2026-09-08

- 团队管理增加时间筛选、加权/未加权 Token 和排序；统一页面时间范围与完整时间显示。
- 新增“保留修改后的额度”开关，默认关闭，下周恢复组织默认额度。
- 管理中心保留当前版本与黄色更新心跳，悬停显示新旧版本，点击不跳转。
- 安装器支持运行目录软链接并保存真实目录，后续升级无需重复输入路径。

已有 Go 环境使用原运维目录升级；Python 历史环境先按[迁移手册](docs/python-to-go-migration.md)处理。

## v2.0.0 - 2026-09-07

首次发布 Go 实现的正式版本，汇总自 `v1.1.5` 及整个 2.0 RC 阶段的改进。

### 主要变化

- 控制面、Gateway、Edge、Web 和后台 Worker 统一为 Go 实现，React 提供管理中心、服务入口和使用中心；以四类不可变镜像交付。
- 产品与仓库统一为 Codex CPA Pool，简称 CCPA；默认新 Key 前缀为 `ccpa_`，客户端环境变量为 `CCPA_API_KEY`，已有配置及现存 Key 保留。
- 配置中心采用分类和子项导航，统一配置项、生效方式、配置值三列；模型倍率与推理倍率分开管理，额度和企业微信通知分别集中设置，表头固定、保存操作常驻底部。
- 统一业务时区；使用中心支持小时、天及小数形式设置登录有效期，范围为 1–12 小时。
- 管理总览和使用中心优化 Token 趋势、加权与未加权口径、模型和推理强度倍率、Top 10 明细高度与账号活跃查询缓存。
- 企业微信报告汇总账号数量、活跃账号和常规周额度状态，头部显示完整统计时间、阈值和使用中心地址；Webhook 保存后展示脱敏地址。
- 管理中心左下角展示当前版本和正式更新提醒，排除 RC/Beta。
- README 与文档产品截图统一为深色模式，使用隔离演示数据。发布流程复用相同源码的验证记录和组件镜像。

### 升级注意

Python `v1.1.5` 不能直接套用普通 Go 升级流程，须先按 [停机保留数据迁移手册](docs/python-to-go-migration.md) 完成离线转换、备份恢复演练与受控接管。已初始化的 Go 环境按 [升级文档](docs/upgrade.md) 操作；旧 RC 的兼容性须结合目标状态确认。保留两份 SQLite、配对主密钥、OAuth 和原 Key。新版本产生业务写入后，不能仅通过还原旧数据库或切回旧镜像降级。

### RC 阶段补充

#### Added

- 个人使用中心新增 7/30/90 天每日用量趋势，支持总用量与“模型 + 推理强度”组合维度，并沿用现有 SQLite 统计口径。
- 增加仅在全部账号无安全迁移目标时可用、需要精确确认的单账号代理投影恢复接口。

#### Changed

- 使用中心趋势按范围与维度独立请求、取消和缓存；图表使用 ECharts，Tooltip 保持单列且最多显示十项。
- 移动端使用中心恢复页面级滚动，趋势收起后立即释放账号明细空间。
- 目标机统一入口由 `deploy.sh` 更名为 `run.sh`；README 提前提供一条可同时用于安装和升级的 `curl` 命令，发布附件、自更新、测试与内部 `make run` 入口同步更名。
- `run.sh` 支持 `curl | sudo sh` 管道启动：内部原子安装脚本并重新连接交互终端，README 不再要求用户管理部署目录。
- README 重构为 GitHub 项目首页，增加主题自适应品牌、状态徽章、产品预览、快速开始、核心能力、精简架构和文档导航。
- `run.sh` 新增 `--tag` Release 选择入口：无参数时列出高于当前部署版本的正式 Releases 并支持交互选择，指定 Tag 时直接选择对应 Release；保留 `--version` 兼容参数。
- Codex CPA Pool 版本提醒改为只比较本地部署版本与 GitHub Latest Release Tag，不再拉取或发布 GHCR release metadata 镜像。

## v1.1.4 - 2026-08-19

### Added

- 面向使用者的快速开始、架构、部署、升级、备份和故障排查文档。
- GitHub 社区协作与安全报告文件。
- 不依赖 GitHub Actions Runner 的本地验证与发布入口。
- SQLite 中的应用发布 `pending`/`applied` 状态与 CPA 候选/已应用镜像身份。
- 权限为 `0600`、由控制面原子生成的 `state/compose.env` Compose 投影。

### Changed

- README 从内部运维手册调整为产品入口，复杂运行细节迁移到 `docs/`。
- GitHub Actions 关闭 Push 和 Pull Request 自动触发，仅保留手动入口。
- 管理页手动刷新会等待新的后端聚合结果，不再继续显示旧缓存状态。
- 应用升级逐个保留业务 CPA 的发布前不可变镜像身份，完整验收后才推进当前版本。
- CPA 镜像界面同时展示语义版本与短 SHA，移动标签只作为显式拉取通道。

## v1.0.14 - 2026-08-18

- 作为 GitHub 原生发布流程改造前的兼容基线。
- 此前 `v1.0.x` 版本的详细变化保留在 Git 提交历史中。
